// app\utils\semanticColor.ts
/** The 6 semantic color tokens defined in app.config.ts's `ui.colors` mapping — the single source of truth for this literal union, don't retype it elsewhere. */
export type SemanticColor = 'primary' | 'secondary' | 'success' | 'info' | 'warning' | 'error'
